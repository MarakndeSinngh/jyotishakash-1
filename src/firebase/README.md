# Firebase Integration Directory

This folder is prepared for Firebase integration (Firestore, Auth, and Storage).
- `config.ts`: Modular Firebase initialization stub prepared for environment variables and future activation.
- Repositories and Services in `/src/repositories/` and `/src/services/` are structured to accept Firebase providers when ready.
